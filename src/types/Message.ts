export type Message = {
    de: string; // O user do personagem que enviou a mensagem
    conteudo: string; // O conteúdo da mensagem
    hora?: string; // Hora em que a mensagem foi enviada, opcional
  };
  